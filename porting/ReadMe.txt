1. 将corss_compile_lib是交叉编译得出的库，放入所需工程中指定的lib文件夹下
2. ChigooWifiInfo.java 以及ChigooWifiLocation.java包名不可以改，否则JNI库会报错
3. ips.ini放到/storage/emulated/0/maptest/CHIGOO下，所有的参数前顶格写，不要加“TAB“缩进
4. WifiLocation.tar是Demo apk代码，供参考
